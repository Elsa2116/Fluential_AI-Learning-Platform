# Backend (FastAPI)

This backend is **folder structure only** (no implementation code yet).

Use this structure when inviting the backend developer so they can start building FastAPI cleanly.

## Planned folders

- `app/api/v1/routes/`
- `app/core/`
- `app/db/`
- `app/models/`
- `app/schemas/`
- `app/services/`
- `tests/`
- `scripts/`
