# AI Learning Platform (Round 2)

Status: ONGOING  
Deadline: 2026-04-25

## Project Structure

- `backend/` — FastAPI backend implementation (auth, courses, AI, learning, chat, payments)
- `frontend/` — Next.js frontend implementation (App Router)

See the full team checklist in `ROUND2_ASSIGNMENT_CHECKLIST.md`.

## Team Workflow

- Use a single shared GitHub repository.

## Quick Start

1. Backend developer works in `backend/`
2. Frontend developer works in `frontend/`
3. Sync API contracts via pull requests

## Frontend Status

- Next.js pages are ready: dashboard, lessons, AI tutor, progress, and quiz.
- Frontend is wired to FastAPI endpoints through `NEXT_PUBLIC_API_BASE_URL`.
